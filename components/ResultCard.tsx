import React from 'react'; import { Copy, CheckCircle2 } from 'lucide-react'; interface ThemeConfig { bg: string; text: string; hex: string; border: string; btn: string; tag: string; } interface ResultCardProps { title: string; subtitle: string; data: string[]; onCopy: () => void; isCopied: boolean; theme: ThemeConfig; isFullHeight?: boolean; } const ResultCard: React.FC<ResultCardProps> = ({ title, subtitle, data, onCopy, isCopied, theme, isFullHeight = false }) => { return ( <div className={`flex flex-col ${isFullHeight ? 'min-h-[200px]' : 'h-[500px]'} bg-white rounded-2xl shadow-sm border transition-all duration-500 ${theme.border} overflow-hidden`}> <div className={`p-4 border-b border-slate-100 flex items-center justify-between`}> <div> <h3 className={`font-bold text-lg transition-colors duration-500 ${theme.text}`}>{title}</h3> <p className="text-xs text-slate-500">{subtitle}</p> </div> <div className={`px-2 py-1 rounded-md text-xs font-semibold transition-colors duration-500 ${theme.tag}`}> {data.length} ชุด </div> </div> <div className={`flex-1 ${isFullHeight ? '' : 'overflow-y-auto'} p-4 bg-slate-50`}> {data.length > 0 ? ( <div className="flex flex-wrap gap-2"> {data.map((num, idx) => ( <span key={idx} className={`inline-flex items-center justify-center w-12 h-10 bg-white border border-slate-200 rounded-lg text-slate-800 font-medium shadow-sm hover:${theme.border} transition-all`} > {num} </span> ))} </div> ) : ( <div className="h-full min-h-[100px] flex flex-col items-center justify-center text-slate-400 opacity-50 space-y-2"> <div className="w-12 h-12 rounded-full border-2 border-dashed border-slate-300"></div> <p className="text-sm">ไม่มีข้อมูลตัวเลข</p> </div> )} </div> <div className="p-4 border-t border-slate-100 mt-auto"> <button disabled={data.length === 0} onClick={onCopy} className={` w-full flex items-center justify-center gap-2 py-3 rounded-xl font-medium transition-all duration-300 ${data.length === 0 ? 'bg-slate-300 text-white cursor-not-allowed' : `${theme.btn} text-white shadow-sm`} `} > {isCopied ? ( <> <CheckCircle2 className="w-5 h-5" /> คัดลอกแล้ว! </> ) : ( <> <Copy className="w-5 h-5" /> คัดลอกทั้งหมด </> )} </button> </div> </div> ); }; export default ResultCard;