import React from 'react'; interface NumberSelectorProps { selectedNumbers: number[]; onToggle: (num: number) => void; themeColor?: string; } const NumberSelector: React.FC<NumberSelectorProps> = ({ selectedNumbers, onToggle, themeColor = 'bg-indigo-600' }) => { const digits = [1, 2, 3, 4, 5, 6, 7, 8, 9, 0]; return ( <div className="grid grid-cols-5 gap-3 sm:gap-4"> {digits.map((num) => { const isSelected = selectedNumbers.includes(num); return ( <button key={num} onClick={() => onToggle(num)} className={`relative flex flex-col items-center justify-center h-16 sm:h-20 rounded-xl text-xl sm:text-2xl font-bold transition-all duration-300 ${isSelected ? `${themeColor} text-white shadow-md scale-105 border-transparent` : 'bg-white text-slate-700 border-2 border-slate-100 hover:border-slate-300 hover:bg-slate-50'}`} > {num} {isSelected && ( <div className="absolute top-1 right-1 bg-white/20 rounded-full p-0.5"> <div className="w-2 h-2 bg-white rounded-full"></div> </div> )} </button> ); })} </div> ); }; export default NumberSelector;